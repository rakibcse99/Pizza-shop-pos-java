<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/pizza_planet_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/pizza_planet_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SoftMethProject5">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".NewOrderActivity"
            android:label="@string/new_order">
        </activity>
        <activity
            android:name=".CurrentOrderActivity"
            android:launchMode="singleTop"
            android:label="@string/current_order">
        </activity>
        <activity
            android:name=".PizzaCustomizerActivity"
            android:label="@string/pizza_customizer">
        </activity>
        <activity
            android:name=".StoreOrderActivity"
            android:label="@string/store_orders">
        </activity>
    </application>

</manifest>